#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"


TEST_CASE( "Extract TAGS", "[morab::extract_images]" )
{
    SECTION( "Extract Tags" )
    {
        const std::string payload = "";

        std::cout << "ok" << std::endl;
    }
}
